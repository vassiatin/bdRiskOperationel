{% extends 'base.html.twig' %}

{% block title %}Element{% endblock %}

{% block body %}
    <h1>Element</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ element.id }}</td>
            </tr>
            <tr>
                <th>CodeElement</th>
                <td>{{ element.codeElement }}</td>
            </tr>
            <tr>
                <th>LibelleElement</th>
                <td>{{ element.libelleElement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_element_index') }}">back to list</a>

    <a href="{{ path('app_element_edit', {'id': element.id}) }}">edit</a>

    {{ include('element/_delete_form.html.twig') }}
{% endblock %}
