{% extends 'base.html.twig' %}

{% block title %}TypeDeReference index{% endblock %}

{% block body %}
    <h1>TypeDeReference index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_de_reference in type_de_references %}
            <tr>
                <td>{{ type_de_reference.id }}</td>
                <td>{{ type_de_reference.description }}</td>
                <td>
                    <a href="{{ path('app_type_de_reference_show', {'id': type_de_reference.id}) }}">show</a>
                    <a href="{{ path('app_type_de_reference_edit', {'id': type_de_reference.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_de_reference_new') }}">Create new</a>
{% endblock %}
