<form method="post" action="{{ path('app_type_de_reference_delete', {'id': type_de_reference.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_de_reference.id) }}">
    <button class="btn">Delete</button>
</form>
