{{ form_start(form) }}

    {{ form_widget(form) }}
    
     <div class="pull-left">
    
    <button type="submit" class="btn btn-outline-info">Enregistrer une categorie</button>
      </div>
{{ form_end(form) }}
